{% extends 'base.html.twig' %}

{% block title %}Estatus de Documentos{% endblock %}

{% block page_title %}
    Estatus de Documentos
{% endblock %}

{% block page_subtitle %}
{% endblock %}

{% block page_content %}
    <div class="row">
        <div class="col-md-10 col-md-offset-1">
            <div class="panel panel-default">
                <div class="panel-body">
                    <div class="panel-content">
                        <div class="table-responsive">
                            <table class="table table-hover">
                                <thead>
                                    <tr>
                                        <th>Id</th>
                                        <th>Estatus</th>
                                        <th>Acciones</th>
                                    </tr>
                                </thead>
                                <tbody>
                                {% for estatus_documento in estatus_documentos %}
                                    <tr>
                                        <td>{{ estatus_documento.idEstatusDocumento }}</td>
                                        <td>{{ estatus_documento.nombreEstatusDocumento }}</td>
                                        <td>
                                            <a class="btn btn-info" href="{{ path('estatus_documento_show', {'idEstatusDocumento': estatus_documento.idEstatusDocumento}) }}">
                                                <i class="fas fa-search-plus"></i>
                                            </a>
                                            <a class="btn btn-info" href="{{ path('estatus_documento_edit', {'idEstatusDocumento': estatus_documento.idEstatusDocumento}) }}">
                                                <i class="fas fa-edit"></i>
                                            </a>
                                        </td>
                                    </tr>
                                {% else %}
                                    <tr>
                                        <td colspan="3">No se encontraron registros.</td>
                                    </tr>
                                {% endfor %}
                                </tbody>
                            </table>
                        </div>
                        <a class="btn btn-primary" href="{{ path('estatus_documento_new') }}">Agregar Nuevo</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
