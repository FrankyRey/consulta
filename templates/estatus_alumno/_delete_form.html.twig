<form method="post" action="{{ path('estatus_alumno_delete', {'idEstatusAlumno': estatus_alumno.idEstatusAlumno}) }}" onsubmit="return confirm('¿Realmente desea eliminar?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ estatus_alumno.idEstatusAlumno) }}">
    <button class="btn btn-danger">
        <span>
            <i class="far fa-trash-alt"></i>
        </span>
        <span>
            Eliminar
        </span>
    </button>
</form>
